# CompIntel Research Frontend

独立 Next.js 前端，用于演示 CompIntel Research 的竞品输入、进度回放、SWOT 可视化、竞品对比与 Markdown 报告。

```bash
npm install
npm run dev
```

默认连接 `http://localhost:8000/ws/compintel`。如后端端口不同，可设置：

```bash
NEXT_PUBLIC_COMPINTEL_API_BASE=http://localhost:8000
```
